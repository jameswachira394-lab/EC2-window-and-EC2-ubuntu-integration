# Institutional Forex Trading System — Requirements (Ubuntu / Docker)
# Install with: pip install -r requirements.txt
#
# NOTE: MetaTrader5 is intentionally NOT listed here.
#       Order execution is delegated to the Windows MT5 Execution Server
#       via HTTP (see connectors/execution_client.py).

# Core data & math
pandas>=2.0.0
numpy>=1.24.0

# HTTP client — talks to the Windows MT5 Execution Server
requests>=2.31.0

# Utilities
python-dateutil>=2.8.2
